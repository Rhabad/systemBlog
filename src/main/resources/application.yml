spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sistem_blog
    username: root
    password: 1234

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update

app:
  jwt-secret: JWTSecretKey
  jwt-expiration-miliseconds: 604800000
